
# las constraseñas no están hasheadas, pero deberían (falta de tiempo)

# el filtro alfabético y por ocupación no estann correctamente implementados, no alcancé a hacer la lógica para que se funcionaran :(

# para intalar los requirements, se debe hacer pip install -r requirements.txt 

# el orden para la creación de la base de datos es:
1) se debe generar la base de datos en tarea2.sql (CREATE DATABASE)
2) se debe crear el usuario en create_user,sql (CREATE USER)
3) se puede proceder a correr cada una del resto de lineas dentro de tarea2.sql
